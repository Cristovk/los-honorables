rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite lectura y escritura a todos los documentos para desarrollo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}